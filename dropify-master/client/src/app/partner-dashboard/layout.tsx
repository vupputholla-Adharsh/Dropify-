export const metadata = {
    title: 'Partner Dashboard | TrafficRelief',
    description: 'Delivery partner dashboard for TrafficRelief',
  };
  
  export default function PartnerDashboardLayout({
    children,
  }: {
    children: React.ReactNode;
  }) {
    return children;
  }